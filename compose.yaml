services:
  service:
    container_name: service
    build:
      context: .
      dockerfile: ./apps/service/Dockerfile
    networks:
      - public
    ports:
      - "3000:3000"
    volumes:
      - storage:/app/data
    restart: always

volumes:
  storage:

networks:
  public:
