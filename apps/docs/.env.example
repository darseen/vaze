# The public origin of the deployed docs site, including the scheme. Read at
# build time, and feeds canonical URLs, Open Graph image URLs, sitemap.xml, and
# robots.txt. Optional locally: it falls back to http://localhost:3001.
NEXT_PUBLIC_SITE_URL=https://vaze.example.com
